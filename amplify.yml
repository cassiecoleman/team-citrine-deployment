version: 1
applications:
  - appRoot: ultra-web
    frontend:
      phases:
        preBuild:
          commands:
            - npm ci
        build:
          commands:
            - printenv >> .env.production
            - npm run build
      artifacts:
        baseDirectory: .next
        files:
          - "**/*"
      cache:
        paths:
          - node_modules/**/*
