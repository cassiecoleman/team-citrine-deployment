export interface ParentAccount {
  id: string;
  name: string;
  email: string;
  phone: string;
}

export interface ChildProfile {
  id: string;
  name: string;
  age: number;
  emergencyContactName: string;
}
